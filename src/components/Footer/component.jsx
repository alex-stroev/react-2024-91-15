import styles from "./styles.module.scss";
export const Footer = () => {
    return (
        <footer className={styles.root}>
            <div className="kernel">Footer</div>
        </footer>
    );
};
